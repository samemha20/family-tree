<!doctype html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>قائمة الشرف</title>
  <script src="/theme.js"></script>
  <link rel="stylesheet" href="/styles.css"/>
</head>
<body>

<header class="topbar">
  <div class="brand">
    <img class="brandLogo" src="/assets/logo.png" alt="شعار العائلة" />
    <div class="brandText">
      <div class="title">قائمة الشرف</div>
      <div class="subtitle">أبرز الرموز والعلماء والشخصيات المؤثرة</div>
    </div>
  </div>

  <nav class="topnav">
    <a class="navLink" href="/">الشجرة</a>
    <a class="navLink" href="/about">نبذة</a>
    <a class="navLink active" href="/honor">قائمة الشرف</a>
    <a class="navLink" href="/tree-pdf">شجرة PDF</a>
    <a class="navLink" href="/support">الدعم</a>
  </nav>

  <div class="actions">
    <button id="themeToggle" class="adminBtn" type="button">الوضع: داكن</button>
    <a class="adminBtn" href="/admin/login">لوحة الإدارة</a>
  </div>
</header>

<main class="page">
  <div class="card">
    <div class="cardHeader"><div style="font-weight:950">الأسماء</div></div>
    <div class="cardBody">
      <div class="honGrid">
        <% if (!items || items.length === 0) { %>
          <div class="muted">لا يوجد عناصر بعد. أضف من لوحة الإدارة → إدارة الشرف.</div>
        <% } %>

        <% for (let i=0; i<items.length; i++){ const x = items[i]; %>
          <div class="card">
            <div class="cardHeader">
              <div style="font-weight:950"><%= x.name %></div>
              <div class="small"><%= x.field || "" %></div>
            </div>
            <div class="cardBody">
              <% if (x.photo_url) { %>
                <img src="<%= x.photo_url %>" alt="<%= x.name %>" class="preview" style="width:100%; height:180px; border-radius:18px; margin-bottom:10px">
              <% } %>
              <div class="muted" style="line-height:1.8"><%= (x.achievement || "").replace(/\n/g,"<br>") %></div>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</main>

<style>
  .honGrid{display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:12px;}
  @media (max-width: 1000px){ .honGrid{grid-template-columns:1fr;} }
</style>

</body>
</html>
